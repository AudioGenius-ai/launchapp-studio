// Export custom hooks for the projects feature
export { useProjectsStore } from '../stores/projectsStore';

// Re-export common hooks from templates
export { useTemplates } from '../templates/hooks/useTemplates';